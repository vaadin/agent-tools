# Go build artifacts. The plugin's native binaries are generated by go/build.sh.
# For local testing they only need to exist on disk (git-ignored is fine). For
# git-based distribution they must instead be committed or shipped as a release
# archive — see the "Distribution" section in README.md.
bin/platform/

# macOS
.DS_Store
